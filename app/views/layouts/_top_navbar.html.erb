<div class="container menu__atas">
	<div class="col-md-12 col-sm-12 col-xs-12">
    <div class="bahasa pull-right col-md-5 col-xs-12 col-sm-6 mobile__login">
      <ul>
      <%= link_to raw(image_tag('img/eng.jpg', class: 'img-responsive', alt: '')), url_for(params.merge(lang: 'en')) %>
    	<%= link_to raw(image_tag('img/ind.jpg', class: 'img-responsive', alt: '')), url_for(params.merge(lang: 'id')) %>
	    <% if logged_in? %>
  		  <li><%= link_to 'Sign Out', logout_accounts_url %></li>
  		<% else %>
  	    <li><%= link_to 'Sign In', login_accounts_url %></li>
      	<li><%= link_to 'Be a Warrior', signup_accounts_url %></li>
      <% end %>
      </ul>
      <%= link_to raw('<i class="fa fa-shopping-cart"></i>'), pages_cart_url %>    	
  	</div>
	</div>
</div>